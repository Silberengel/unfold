# Shared hub MySQL. Copy to .env on the server (keep secret).
MYSQL_DATABASE=unfold_db
MYSQL_VERSION=8.0.36
MYSQL_CHARSET=utf8mb4
MYSQL_USER=unfold_user
MYSQL_PASSWORD=change-me
MYSQL_ROOT_PASSWORD=change-me-root
